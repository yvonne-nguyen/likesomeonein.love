<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} · {% endif %}likesomeonein.love</title>
  <meta name="description" content="{{ description or 'A personal collection of writing, photos, and fragments.' }}">
  <link rel="stylesheet" href="{{ prefix }}/css/style.css">
  <link rel="alternate" type="application/rss+xml" title="likesomeonein.love" href="{{ prefix }}/feed.xml">
</head>
<body>
  <div class="page">
    <header class="site-header">
      <a class="site-title" href="{{ prefix }}/">likesomeonein.love</a>
      <nav class="site-nav">
        <a href="{{ prefix }}/archive/">Archive</a>
        <a href="{{ prefix }}/tags/">Tags</a>
        <a href="{{ prefix }}/about/">About</a>
      </nav>
    </header>

    <main class="site-main">
      {{ content | safe }}
    </main>
  </div>
</body>
</html>
